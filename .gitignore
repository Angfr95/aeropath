# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# Environment files
.env

# Generated seed files
scripts/seed/seed_complete.sql

# Node.js
node_modules/
web/frontend-react/node_modules/

# Build output
web/frontend-react/dist/
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Deployment scripts (local)
deploy-vps.sh
setup-vps.sh

# Secret setup scripts (contain tokens/keys — never commit)
scripts/set_gh_secrets*
scripts/set_github_secret*

# Kubernetes secrets (inject via kubectl create secret or Sealed Secrets)
k8s/secrets.yaml
k8s/secrets.local.yaml

# SSH keys / deploy keys (never commit)
*_deploy_key
*_deploy_key.pub
tmp_key.pem
tmp_key

# Bug reports / debug docs (internal only)
docs/bug-auth-405-nginx-reload.md
_deploy_key.pem  
api-gateway-linux  
